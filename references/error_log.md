# 错误日志 (Error Log)

> 跨会话积累的 AI 操作错误记录。每个 Step 的 agent 在收尾检查时如果发现新错误，
> 必须追加到此文件。Step 8（润色）的"术语统一"检查发现术语不一致时也记录于此。
>
> 设计原则：**不从零开始——每次错误都是一次学习机会。**
> 借鉴来源：ResearchWiki `memory/error_log.md`。

## 记录格式

每条错误记录包含以下字段：

| 字段 | 说明 |
|------|------|
| **日期** | 发现日期 (YYYY-MM-DD) |
| **来源 Step** | 哪个步骤发现的 (Step 1-8 / 通用) |
| **Error** | 错误的具体表现（用户可观察到的现象） |
| **Cause** | 根本原因分析（为什么 AI 会犯这个错误） |
| **Correction rule** | 为避免重复，在对应 agent 文件或本文件中添加的规则 |
| **Impact** | 对产出的影响（低/中/高/关键） |
| **Fixed** | 是否已修复（✅ 是 / 🔧 部分 / ❌ 否） |

---

## 错误记录

<!-- 
格式模板（新记录追加到下方）：

### YYYY-MM-DD | Step X | 影响等级
- **Error:** 
- **Cause:** 
- **Correction rule:** 
- **Impact:** 
- **Fixed:** 

每个 Step 的 agent 在收尾检查时必须问自己：
"本轮执行中是否出现了我本应避免但没避免的错误？"
如有，追加一条记录。
-->

### 2026-06-05 | — | —
- **Error:** （初始创建，无错误记录）
- **Cause:** —
- **Correction rule:** —
- **Impact:** —
- **Fixed:** —

---

## 使用规则

1. **所有 agent 启动时读取本文件** — 检查是否有已知错误规则适用于当前任务
2. **每个 agent 收尾时写入本文件** — 如果本轮出现新错误或发现新规则缺口
3. **Step 7h（引用审计）发现误引模式时** — 追加到本文件，关联到 `references/citation-audit-guide.md`
4. **Step 8（润色）发现系统性 AI 痕迹时** — 追加到本文件，关联到 Level 3 去 AI 化规则
5. **下载失败的新模式** — 追加到本文件，关联到 `config/publishers.toml` 或 `references/publisher-access-matrix.md`
6. **不记录一次性/偶发性错误** — 只记录可能重复出现的模式性错误
